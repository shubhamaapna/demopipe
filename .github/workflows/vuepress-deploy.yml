name: Build and Deploy
on: [push]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@master 
      
    - name: Debug
      run: ls
    - name: hosting
      run: |
        set -e
        yarn && yarn build
        ls 
        cd docs/.vuepress/dist
        git init
        git add -A
        git commit -m 'deploy'
        git push -f git@github.com:shubhamaapna/demopipe.git master:gh-pages
        cd -
